.. _frameworks-java-spring-mvc-annotation-controller-label:

Controller
==========
- annotation used to specify a component is a "controller"

    .. code-block:: python
        :linenos:

        // need a controller method to read form data and
        // add data to the model

        @Controller
        public DemoController {
            ...
            // methods
        }


:ref:`Go Back <frameworks-java-spring-mvc-annotation-label>`.
