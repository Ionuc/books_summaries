.. _rest-rest-methods-label:

Rest Methods
============

1. GET
------
- The GET method is used to 'retrieve' a record or a collection of records from the server


2. POST
-------
- The POST method sends data to create a 'new record' on the server

3. PUT
------
- The PUT method replaces all current representations of the target resource with the request content

4. PATCH
--------
- The PATCH method applies partial modifications to a resource instead of replacing it

5. DELETE
---------
- The DELETE method deletes the specified resource

:ref:`Go Back <rest-label>`.